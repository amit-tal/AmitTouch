(function(){'use strict';
const style=document.createElement('style');style.textContent=`#adminBody .admin-home-canonical .ah-actions button{pointer-events:auto!important;position:relative!important;z-index:20!important}`;document.head.appendChild(style);
function cardData(){const card=document.querySelector('#adminBody .admin-home-canonical .ah-next');if(!card)return null;const name=card.querySelector('.ah-next-copy strong')?.textContent?.trim()||'לקוחה';const service=card.querySelector('.ah-next-copy span')?.textContent?.trim()||'טיפול';const duration=card.querySelector('.ah-next-copy small')?.textContent?.trim()||'';const start=card.querySelector('.ah-time')?.textContent?.trim()||'';const avatar=card.querySelector('.ah-avatar img')?.getAttribute('src')||'';const list=window.AMIT_ADMIN_CALENDAR_APPOINTMENTS||[];const picked=list.find(a=>String(a.name||a.customer_name||a.customerName||'').trim()===name&&(String(a.start||a.start_time||'').trim()===start||!start))||list.find(a=>String(a.name||a.customer_name||a.customerName||'').trim()===name)||null;return picked||{name,service,start,end:'',date:'',phone:'',status:'confirmed',duration,avatar,source:'אפליקציה'};}
function isDetailsButton(target){const btn=target?.closest?.('#adminBody button');return btn&&btn.textContent.replace(/\s+/g,' ').trim()==='פרטי תור'?btn:null;}
function openNow(ev){const btn=isDetailsButton(ev.target);if(!btn)return;ev.preventDefault();ev.stopPropagation();ev.stopImmediatePropagation?.();const a=cardData();if(typeof window.AMIT_ADMIN_OPEN_APPOINTMENT_DETAILS==='function'){window.AMIT_ADMIN_OPEN_APPOINTMENT_DETAILS(a,'home');return}setTimeout(()=>{if(typeof window.AMIT_ADMIN_OPEN_APPOINTMENT_DETAILS==='function')window.AMIT_ADMIN_OPEN_APPOINTMENT_DETAILS(a,'home')},0);}
['pointerdown','touchstart','click'].forEach(type=>document.addEventListener(type,openNow,true));
function hardBind(){document.querySelectorAll('#adminBody button').forEach(btn=>{if(btn.textContent.replace(/\s+/g,' ').trim()!=='פרטי תור')return;btn.type='button';btn.dataset.go='appointment-details';btn.style.pointerEvents='auto';btn.style.position='relative';btn.style.zIndex='20';btn.onclick=function(ev){openNow(ev);return false};});}
new MutationObserver(hardBind).observe(document.body,{childList:true,subtree:true});window.addEventListener('pageshow',hardBind);window.addEventListener('amit:session-ready',hardBind);setInterval(hardBind,500);hardBind();
})();