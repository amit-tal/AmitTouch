(function(){
 const STYLE_ID='amit-admin-approval-restore-style';
 document.getElementById(STYLE_ID)?.remove();
 const style=document.createElement('style');style.id=STYLE_ID;style.textContent=`
 #adminBody .admin-actions{display:grid!important;grid-template-columns:1fr 1fr!important;gap:9px!important;margin-top:11px!important}
 #adminBody .admin-actions button{min-height:44px!important;border-radius:12px!important;font-size:12px!important;font-weight:600!important}
 #adminBody .admin-approve{border:0!important;background:#2f716b!important;color:#fff!important}
 #adminBody .admin-reject{border:1px solid rgba(176,105,94,.18)!important;background:#fff4f1!important;color:#a76459!important}
 `;document.head.appendChild(style);
 function info(a){const c=a.customer||{};return{name:[c.first_name,c.last_name].filter(Boolean).join(' ')||'לקוחה',phone:c.phone||'',when:new Date(a.starts_at).toLocaleString('he-IL',{dateStyle:'short',timeStyle:'short'}),service:a.service_name||'',price:Number(a.total_price||0)};}
 window.adminApproveAppointment=async function(id){if(!confirm('לאשר את התור?'))return;try{const r=await fetch('/api/admin/appointments/'+encodeURIComponent(id)+'/approve',{method:'POST'});const d=await r.json();if(!r.ok)throw new Error(d.error||'APPROVAL_FAILED');alert('התור אושר');await window.renderAdminAppointments();}catch(e){console.error(e);alert('לא הצלחתי לאשר את התור');}};
 window.adminRejectAppointment=async function(id){if(!confirm('לדחות את בקשת התור?'))return;try{const r=await fetch('/api/admin/appointments/'+encodeURIComponent(id)+'/reject',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({reason:'בקשת התור לא אושרה על ידי עמית'})});const d=await r.json();if(!r.ok)throw new Error(d.error||'REJECTION_FAILED');alert('בקשת התור נדחתה');await window.renderAdminAppointments();}catch(e){console.error(e);alert('לא הצלחתי לדחות את הבקשה');}};
 window.renderAdminAppointments=async function(){const target=document.getElementById('adminBody');if(!target)return;target.innerHTML='<div class="card glass"><p class="subtitle">טוענת תורים…</p></div>';try{const response=await fetch('/api/admin/appointments',{cache:'no-store'});const data=await response.json();if(!response.ok)throw new Error(data.error||'ADMIN_APPOINTMENTS_FAILED');const rows=(data.appointments||[]).filter(a=>a.status!=='cancelled');const pending=rows.filter(a=>a.status==='pending');const confirmed=rows.filter(a=>a.status==='confirmed');const p=pending.length?'<div class="section-title">ממתינים לאישור</div>'+pending.map(a=>{const x=info(a);return `<div class="notice glass"><h3>${x.name}</h3><p>${x.service}<br>${x.when}<br>${x.phone}<br>₪${x.price}</p><div class="admin-actions"><button class="admin-approve" onclick="adminApproveAppointment('${a.id}')">אישור</button><button class="admin-reject" onclick="adminRejectAppointment('${a.id}')">דחייה</button></div></div>`}).join(''):'<div class="card glass"><p class="subtitle">אין בקשות שממתינות לאישור.</p></div>';const c=confirmed.length?'<div class="section-title">תורים מאושרים</div>'+confirmed.map(a=>{const x=info(a);return `<div class="notice glass"><h3>${x.name}</h3><p>${x.service}<br>${x.when}<br>${x.phone}<br>₪${x.price}</p></div>`}).join(''):'';target.innerHTML=p+c;}catch(e){console.error(e);target.innerHTML='<div class="card glass">לא הצלחתי לטעון את התורים</div>';}};
 const previous=window.renderAdmin;
 window.renderAdmin=window.renderAdminAppointments;
 document.addEventListener('click',function(e){const admin=e.target.closest('[onclick*="renderAdmin"],[onclick*="show(\'admin\')"],[onclick*="show(&quot;admin&quot;)"]');if(admin)setTimeout(()=>window.renderAdminAppointments(),0);},true);
})();